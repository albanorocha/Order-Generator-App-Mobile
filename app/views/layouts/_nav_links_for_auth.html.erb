<% if user_signed_in? %>
  <% if current_user.try(:admin?) %>
    <li><%= link_to 'Users', users_path %></li>
  <% end %>
  <li><%= link_to 'Sign out', destroy_user_session_path, :method=>'delete' %></li>
  <p class="navbar-text">
    Bem vindo: <%= link_to current_user.name, current_user %>
  </p>
<% end %>
