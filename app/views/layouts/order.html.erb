<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Orçamento CRAOSX" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Orçamento CRAOSX" %>">

    <link href='http://fonts.googleapis.com/css?family=Lato:100,300,400,700,900,100italic,300italic,400italic,700italic,900italic' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Arvo:400,700' rel='stylesheet' type='text/css'>

    <%= stylesheet_link_tag 'order', media: 'all' %>
    <%= favicon_link_tag "favicon.png"%>
    <%= javascript_include_tag 'order' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <%= render 'layouts/messages' %>
    <%= yield %>

    <footer>
      <div class="container">
        <p class="text-muted text-center">
          © 2016 · CRAOSX · Fazemos projetos vivos
        </p>
      </div>
    </footer>

  </body>
</html>
